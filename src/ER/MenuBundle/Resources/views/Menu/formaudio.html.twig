

  
 
      <div class="grid-x align-center">
	    <div class="cell medium-8">
  
 {{ form_start(form) }}

  {# Les erreurs générales du formulaire. #}
  {{ form_errors(form) }}

       
			   
			   
	 {{ form_row(form.jours) }}	
	 
	  
        <div>
		 
    {# Génération du label. #}
    {{ form_label(form.heure) }}

    {# Affichage des erreurs pour ce champ précis. #}
    <span class="error">{{ form_errors(form.heure) }}</span>
         
               <div>
              {# Génération de l'input. #}
            {{ form_widget(form.heure) }}
             </div>
        </div>
		
		
		<div>
    {# Génération du label. #}
    {{ form_label(form.minute) }}

    {# Affichage des erreurs pour ce champ précis. #}
    <span class="error">{{ form_errors(form.minute) }}</span>
       
               <div>
              {# Génération de l'input. #}
            {{ form_widget(form.minute) }}
             </div>
        </div>
	
		<div>
    {# Génération du label. #}
    {{ form_label(form.volumeMax, "Volume sonore maximum du réveil (%)") }}

    {# Affichage des erreurs pour ce champ précis. #}
    <span class="error">{{ form_errors(form.volumeMax) }}</span>

               <div>
              {# Génération de l'input. #}
            {{ form_widget(form.volumeMax) }}
             </div>
        </div>
			         
          <br/>
          
		 {# Génération automatique des champs pas encore écrits.
     Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
     et tous les champs cachés (type « hidden »). #}
     <div>{{ form_rest(form) }}</div>	

		 {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
      {{ form_widget(form.Valider) }}
       {% if listAudios %}	
		 {% for audio in listAudios %}
		<a href="{{ path('menu_audio_view', {'id': audio.id}) }}" class="button medium">Annuler</a>
		 {% endfor %}
		{% else %}
		<a href="{{ path('menu_verif_audio')}}" class="button medium">Annuler</a>
		{% endif %}
  
  
  
 
  

  {# Fermeture de la balise <form> du formulaire HTML #}
  {{ form_end(form) }}


     

	   </div>
	  </div>
	

	
